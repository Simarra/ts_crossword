import { Direction } from './directions';
export class Cell {
    letter: string;
    idx: number;
    direction: string|Direction;
    // constructor(letter: string) {
    //     if (letter === '') {
    //         this.letter = '';}
    // }
}